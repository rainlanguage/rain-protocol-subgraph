import { BigInt } from '@graphprotocol/graph-ts'
let ZERO_BI: BigInt = BigInt.fromI32(0)
let ONE_BI: BigInt = BigInt.fromI32(1)
let ZERO_ADDRESS = "0x0000000000000000000000000000000000000000"
export {
    ZERO_BI,
    ONE_BI,
    ZERO_ADDRESS
}